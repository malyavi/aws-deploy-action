{
  "name": "aws-deploy-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action deploying a serverless stack to every region a stage spans",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "license": "MIT"
}
